import { layout,metadata,escapeHtml as e } from './seo.mjs';
import { services,researchProviders } from '../content/services.mjs';
import { WHATSAPP_URL } from '../src/contact.js';
export const toolDefinitions=[
 ['references','Préparer une référence','Saisis les métadonnées d’un document et exporte une référence RIS ou BibTeX pour ton gestionnaire.','guides/zotero-bibliographie'],
 ['planning','Planifier mon mémoire','Transforme ta date de dépôt en une trame de jalons modifiable, à confronter à ton calendrier réel.','guides/planification-memoire'],
 ['texte','Mesurer mon texte','Compte les mots et les caractères d’un texte, localement dans ton navigateur.','guides/resume-scientifique'],
 ['citations','Vérifier mes citations','Suis une grille de relecture des sources et repère ce qui demande une vérification.','guides/citations-plagiat']
].map(([id,title,description,guide])=>({id,title,description,guide}));
export const toolsPublicPaths=['/outils','/recherche','/services','/tarifs',...toolDefinitions.map(t=>'/outils/'+t.id),...services.map(s=>'/services/'+s.slug)];
const hero=(title,description,kicker='Outils et méthodes')=>`<section class="record-hero"><p class="eyebrow">${e(kicker)}</p><h1>${e(title)}</h1><p class="article-lead">${e(description)}</p></section>`;
const wrap=(context,path,title,description,content,script=false)=>layout({title,description,meta:metadata({...context,path,title,description}),content:`<main id="contenu">${content}</main>${script?'<script src="/assets/research-tools.js" type="module"></script>':''}`});
const serviceCard=s=>`<a class="guide-card" href="/services/${s.slug}"><span class="eyebrow">Espace projet</span><h3>${e(s.title)}</h3><p>${e(s.description)}</p><span class="card-link">Voir ce module →</span></a>`;
export function renderServices(context){const title='Des outils pour chaque étape de ton projet',description='Du cadrage à la soutenance : découvre les informations à préparer, le résultat attendu et les limites de chaque module.';return wrap(context,'/services',title,description,hero(title,description,'Mémoire · PFE · Doctorat · Enseignement')+`<section class="library-content"><div class="guide-grid">${services.map(serviceCard).join('')}</div><section class="record-reading"><h2>Tu gardes la main sur ton travail</h2><p>Les modules de l’espace utilisent une assistance automatisée. Tu fournis le contexte et les sources, tu vérifies les propositions et tu conserves ta version. La bibliothèque, les outils simples et les modèles restent accessibles sans abonnement.</p><a class="site-button" href="/tarifs">Comparer les offres</a> <a href="/outils">Essayer les outils gratuits →</a></section></section>`);}
export function renderService(s,context){return wrap(context,'/services/'+s.slug,s.title,s.description,hero(s.title,s.description,'Module de ton espace personnel')+`<section class="service-layout"><article class="article-body"><h2>Ce que tu prépares</h2><p>${e(s.input)}</p><h2>Ce que tu obtiens</h2><p>${e(s.output)}</p><h2>Comment avancer</h2><ol><li>Crée ou ouvre ton projet et renseigne les consignes de ton cursus.</li><li>Ajoute les informations et les sources demandées par le module.</li><li>Lance une étape, relis le résultat et modifie ta version.</li><li>Garde le document dans ton compte ou exporte-le pour continuer.</li></ol><h2>Les vérifications à garder</h2><p>${e(s.boundary)}</p><p><a href="/${s.guide}">Lire la méthode avant de commencer →</a></p></article><aside class="reading-card"><span class="eyebrow">Commencer sur mon sujet</span><h2>Essaie gratuitement.</h2><p>1 projet et 3 générations par mois, partagées entre les modules. Chaque génération réussie utilise une unité.</p><a class="site-button" href="/?outil=${s.id}#inscription">Ouvrir ce module dans mon projet</a><p><a href="/tarifs">Essentiel 19 € · Signature 29 € / mois</a></p><p class="fine">Le module utilise une assistance automatisée ; aucun résultat n’est validé à ta place.</p></aside></section>`);}
export function renderResearch(context){const title='Les bons outils pour trouver et organiser ses sources',description='Accède aux moteurs académiques et aux guides de recherche adaptés à ton besoin. Chaque service garde ses propres résultats et conditions.';return wrap(context,'/recherche',title,description,hero(title,description,'Annuaire de recherche')+`<section class="library-content"><form data-tool="search" class="tool-form" hidden><label>Ton sujet ou tes mots-clés<input name="q" required maxlength="240" placeholder="Par exemple : tutorat universitaire"></label><label>Rechercher dans<select name="provider"><option value="scholar">Google Scholar</option><option value="hal">HAL</option><option value="pubmed">PubMed</option><option value="theses">theses.fr</option></select></label><button class="site-button" type="submit">Ouvrir la recherche ↗</button><p class="fine">Tu ouvriras le site choisi avec tes mots-clés. Les résultats ne sont pas copiés ici.</p></form><div class="provider-grid">${researchProviders.map(p=>`<article class="provider-card"><span class="provider-monogram" aria-hidden="true">${p.short}</span><span class="eyebrow">${e(p.label)}</span><h2>${e(p.name)}</h2><p>${e(p.description)}</p><a class="site-button" href="${p.url}" target="_blank" rel="noopener noreferrer">Ouvrir ${e(p.name)} ↗</a><a href="/${p.guide}">Lire notre guide de méthode →</a></article>`).join('')}</div><p class="fine">Liens vers des services indépendants. Leurs noms servent à les identifier ; aucune affiliation ni certification de Soutenance Pro n’est revendiquée.</p></section>`,true);}
export function renderTools(context){const title='Des outils de recherche utiles, sans détour',description='Prépare tes références, planifie ton mémoire et vérifie ton texte. Ces outils simples fonctionnent gratuitement dans ton navigateur.';return wrap(context,'/outils',title,description,hero(title,description,'Outils gratuits · Sans inscription')+`<section class="library-content"><div class="guide-grid">${toolDefinitions.map(t=>`<a class="guide-card" href="/outils/${t.id}"><span class="eyebrow">Gratuit · Navigateur</span><h3>${e(t.title)}</h3><p>${e(t.description)}</p><span class="card-link">Utiliser l’outil →</span></a>`).join('')}</div><section class="record-reading"><h2>Besoin de travailler sur ton propre projet ?</h2><p>L’espace personnel rassemble le plan, les sources, les versions et les outils avancés. L’offre gratuite permet de produire trois résultats par mois.</p><a href="/services">Découvrir les modules de l’espace →</a></section></section>`);}
const referenceForm=`<form class="tool-form" data-tool="references" hidden><label>Type<select name="type"><option value="ART">Article</option><option value="THESE">Thèse</option><option value="MEM">Mémoire</option><option value="REPORT">Rapport</option><option value="OTHER">Autre document</option></select></label><label>Titre du document<input name="title" required maxlength="1200"></label><label>Auteurs, un par ligne<textarea name="authors" rows="3" maxlength="4000" required placeholder="Recopie les noms dans l’ordre du document"></textarea></label><label>Année<input name="year" type="number" min="1000" max="2100"></label><label>Revue, si applicable<input name="journal" maxlength="500"></label><label>DOI, si disponible<input name="doi" maxlength="250" placeholder="10.…"></label><label>Lien stable vers le document<input name="url" type="url" required maxlength="1500" placeholder="https://…"></label><button class="site-button" type="submit">Préparer ma référence</button><div data-output hidden><label>Référence de travail<textarea name="reference" rows="5" readonly></textarea></label><p class="fine">Cette présentation lisible n’est pas une certification APA ou Vancouver. Choisis ensuite le style demandé dans ton gestionnaire.</p><div class="tool-actions"><button type="button" data-export="ris">Télécharger RIS</button><button type="button" data-export="bib">Télécharger BibTeX</button></div></div><p data-status role="status" aria-live="polite"></p></form>`;
const planningForm=`<form class="tool-form" data-tool="planning" hidden><label>Date de début<input name="start" type="date" required></label><label>Date de dépôt<input name="end" type="date" required></label><button class="site-button" type="submit">Créer une trame de calendrier</button><p class="fine">Les jalons sont une suggestion de répartition, sans connaissance de tes autorisations, de ton terrain ou des délais de ton encadrant.</p><div data-output hidden><div class="tool-table"><table><thead><tr><th>Jalon</th><th>Date proposée</th></tr></thead><tbody></tbody></table></div><button data-export="csv" type="button">Télécharger le calendrier CSV</button></div><p data-status role="status" aria-live="polite"></p></form>`;
const textForm=`<form class="tool-form" data-tool="text" hidden><label>Ton texte<textarea name="text" maxlength="200000" rows="14" placeholder="Colle le passage à mesurer"></textarea></label><output class="text-statistics" aria-live="polite"></output><p class="fine">Comptage indicatif : les conventions peuvent différer de celles de ton traitement de texte. Le texte reste dans cette page et n’est ni envoyé ni enregistré.</p></form>`;
const checks=['Chaque idée empruntée est reliée à sa source.','Les extraits exacts ont des guillemets et un emplacement.','J’ai comparé mes reformulations au passage original.','Chaque référence contient les informations du document réel.','Les tableaux, figures et images ont une source et des droits vérifiés.','Le texte et la bibliographie se correspondent après la dernière révision.'];
const citationForm=`<form class="tool-form" data-tool="citations" hidden>${checks.map((c,i)=>`<label class="citation-check"><input type="checkbox" name="check-${i}"><span>${e(c)}</span></label>`).join('')}<output aria-live="polite">0 point vérifié sur 6</output><button type="reset">Recommencer la vérification</button></form><section class="record-reading"><h2>Besoin d’une analyse de similitudes ?</h2><p>Cette grille aide à relire les citations. Elle ne compare pas ton texte à une base de publications et ne délivre aucun certificat. Pour une analyse externe, utilise le service autorisé par ton établissement ; ses règles, ses coûts et ses conditions de confidentialité s’appliquent.</p><p><a href="https://www.turnitin.com/" target="_blank" rel="noopener noreferrer">Turnitin ↗</a> · <a href="https://www.compilatio.net/" target="_blank" rel="noopener noreferrer">Compilatio ↗</a></p><p>Un score de similitudes ou un indicateur de rédaction automatique demande une interprétation. Aucun résultat « 100 % sans IA » n’est garanti ici.</p></section>`;
export function renderTool(t,context){return wrap(context,'/outils/'+t.id,t.title,t.description,hero(t.title,t.description,'Outil gratuit · Sans compte')+`<section class="tool-layout"><div>${({references:referenceForm,planning:planningForm,texte:textForm,citations:citationForm})[t.id]}<noscript><p>Active JavaScript pour utiliser cet outil local, ou consulte le guide associé ci-dessous.</p></noscript></div><aside class="reading-card"><h2>La méthode derrière l’outil</h2><p>Un outil facilite une étape. Les données et les références restent à vérifier dans leur contexte.</p><a href="/${t.guide}">Lire le guide pratique →</a><a href="/bibliotheque">Explorer la bibliothèque →</a></aside></section>`,true);}
export function renderPricing(context){const title='Tarifs : Découverte, Essentiel et Signature',description='Bibliothèque gratuite et espace personnel : 3, 60 ou 150 générations par mois. Compare les limites de chaque offre en euros.';const plans=[['free','Découverte',0,1,3],['offre','Essentiel',19,5,60],['max','Signature',29,20,150]];return wrap(context,'/tarifs',title,description,hero('Une bibliothèque ouverte. Un espace à ton rythme.','Consulte les ressources librement. Choisis ensuite le volume adapté au travail dans ton espace personnel.','Les offres Soutenance Pro')+`<section class="library-content"><div class="plans-grid">${plans.map(([id,name,price,projects,generations])=>`<article class="plan-card ${id==='max'?'recommended':''}">${id==='max'?'<span class="plan-badge">Recommandé</span>':''}<h2>${name}</h2><p class="plan-price">${price} € <small>${price?'/ mois':''}</small></p><p>${price?'Abonnement mensuel · prochainement':'Gratuit · sans carte bancaire'}</p><ul><li>${projects} projet${projects>1?'s':''}</li><li>${generations} générations par mois</li><li>9 modules de travail</li><li>Documents enregistrés et modifiables</li><li>Exports Word et PowerPoint</li></ul><a class="site-button" href="${price?'/?offre='+id+'#inscription':'/#inscription'}">${price?'Choisir '+name:'Créer mon compte gratuit'}</a><p class="fine">${price?'Prépare ton compte. L’activation payante sera disponible après ouverture des paiements.':'Commence ton projet sans attendre un email de confirmation.'}</p></article>`).join('')}</div><section class="record-reading"><h2>Des limites lisibles</h2><p>La bibliothèque, les modèles Word et les outils simples restent gratuits. Une génération correspond à un résultat produit par un module de l’espace. Les quotas se renouvellent par mois civil, à 00 h UTC, et sont partagés entre les projets du compte.</p><p>À la limite, les nouvelles générations s’arrêtent. Tu gardes l’accès à tes documents pour les lire, les modifier et les exporter. Les offres payantes ne sont pas encore activées ; aucun bouton de cette page ne débite ta carte.</p><p><a href="${WHATSAPP_URL}" target="_blank" rel="noopener noreferrer">Une question sur une offre ? Écris-nous sur WhatsApp ↗</a></p></section></section>`);}
