import { renderOwnedLibrary } from './library-content.mjs';
import { renderExtendedLibrary } from './extended-library.mjs';
import { documents, snapshot } from '../content/documents.mjs';
import { disciplines, documentTypes } from '../content/disciplines.mjs';
import { pages, templateResources, resourceTopic } from '../content/catalog.mjs';
import { escapeHtml as e, layout, metadata } from './seo.mjs';
import { referenceText, referenceRIS, referenceBib } from '../shared/reference-format.js';
const number=n=>n.toLocaleString('fr-FR');
export const documentPath=d=>'/bibliotheque/document/'+d.id;
export const disciplinePath=d=>'/bibliotheque/discipline/'+d.id;
export const pagePath=n=>n===1?'/bibliotheque/documents':'/bibliotheque/documents/page/'+n;
export const cataloguePageCount=Math.ceil(documents.length/50);
export const documentPublicPaths=[...disciplines.map(disciplinePath),...Array.from({length:cataloguePageCount},(_,i)=>pagePath(i+1)),...documents.map(documentPath)];
export const documentIndexPaths=disciplines.map(disciplinePath).concat(pagePath(1));
const authorShort=d=>d.authors.slice(0,3).join(', ')+(d.authors.length>3?' et al.':'');
const subject=d=>disciplines.find(s=>s.id===d.discipline);
const a=(href,label)=>`<a href="${e(href)}" target="_blank" rel="noopener noreferrer">${e(label)} ↗</a>`;
export function documentCard(d){return `<article class="document-card"><div class="document-spine" aria-hidden="true">${e(documentTypes[d.type])}<span>${d.year}</span></div><div><p class="resource-type">${e(subject(d).label)} · Français</p><h3><a href="${documentPath(d)}">${e(d.title)}</a></h3><p class="document-authors">${e(authorShort(d))}</p><p class="document-origin">Document chez HAL · ${e(documentTypes[d.type])}</p><a class="resource-link" href="${documentPath(d)}">Voir la notice et le document <span aria-hidden="true">→</span></a></div></article>`;}
export function searchEntries(){return [...pages.map(p=>({id:p.slug,title:p.heading,authors:['Soutenance Pro'],year:2026,type:'guide',format:'guide',discipline:'method',disciplines:resourceTopic(p).includes('health')?['method','medecine','sante-publique']:['method'],label:p.category,path:'/'+p.slug,keywords:p.body.replace(/https?:\/\/\S+/g,'').slice(0,16000),description:p.lead})),...templateResources.map((t,i)=>({id:'template-'+i,title:t.title,authors:['Soutenance Pro'],year:2026,type:'template',format:'template',discipline:'method',label:'Modèle Word',path:t.path,keywords:t.keywords,description:t.description})),...documents.map(d=>({id:d.id,title:d.title,authors:d.authors.slice(0,5),year:d.year,type:d.type,format:'document',discipline:d.discipline,label:subject(d).label,path:documentPath(d),keywords:d.keywords.join(' '),description:documentTypes[d.type]+' · Document hébergé par HAL',doi:d.doi,url:d.url}))];}
export function renderExplorer(context){
 const title='Bibliothèque académique : documents, guides et modèles',description=`Lis nos guides complets pour le PFE, le mémoire et le stage. Travaille tes références et tes notes dans la bibliothèque, avec recherche par discipline et année.`;
 const initial=documents.slice(0,6);
 const options=disciplines.map(d=>`<option value="${d.id}">${e(d.label)}</option>`).join('');
 const content=`<main id="contenu"><section class="library-hero"><div class="library-intro"><span class="eyebrow">La bibliothèque Soutenance Pro</span><h1>Trouve ce qui nourrit<br>ton prochain travail.</h1><p>Des guides complets à lire ici, des modèles à adapter et un espace pour tes lectures. Travaille tes références et tes notes sans quitter la bibliothèque ; retrouve aussi les textes des auteurs sur leur dépôt.</p><div class="library-facts"><span>${number(documents.length)} documents référencés</span><span>${disciplines.length} disciplines</span><span>${pages.length} guides et parcours</span><span>${templateResources.length} modèles Word</span></div></div><figure class="library-photo"><img src="/assets/library-reading-room.webp" alt="" width="1536" height="1024" decoding="async"><figcaption>Un espace pour tes lectures et ton travail.</figcaption></figure></section>
 <section class="library-content library-search-shell" data-catalogue><nav class="library-pathways" aria-label="Explorer par besoin"><a href="#ressources-soutenance-pro">Guides à lire ici →</a><a href="#modeles">Modèles Word →</a><a href="#parcours-pays">Ressources par pays →</a><a href="/bibliotheque/documents">Documents de recherche →</a><a href="/recherche">Moteurs académiques →</a></nav>
 <nav class="catalogue-modes" data-catalogue-modes hidden aria-label="Choisir la collection"><button type="button" data-scope="local" aria-pressed="true">Guides &amp; sélection <span>${number(documents.length + pages.length + templateResources.length)} ressources</span></button><button type="button" data-scope="hal" aria-pressed="false">Archives HAL <span>Recherche étendue</span></button></nav>${renderExtendedLibrary()}<div data-local-panel><form class="library-search catalogue-filters" data-catalogue-form role="search" hidden><div class="catalogue-query"><label for="catalogue-query">Sujet, titre, auteur ou DOI</label><input id="catalogue-query" name="q" type="search" maxlength="240" placeholder="Mémoire, pédagogie, santé publique…"></div><div><label for="catalogue-discipline">Discipline</label><select id="catalogue-discipline" name="discipline"><option value="all">Toutes les disciplines</option><option value="method">Méthodes et guides</option>${options}</select></div><div><label for="catalogue-format">Ressource</label><select id="catalogue-format" name="format"><option value="all">Tous les formats</option><option value="document">Documents de recherche</option><option value="guide">Guides et parcours</option><option value="template">Modèles Word</option></select></div><div><label for="catalogue-type">Type de document de recherche</label><select id="catalogue-type" name="type"><option value="all">Tous les types</option>${Object.entries(documentTypes).map(([k,v])=>`<option value="${k}">${e(v)}</option>`).join('')}</select></div><div><label for="catalogue-year">Année du document de recherche</label><select name="year" id="catalogue-year"><option value="all">Toutes les années</option>${[...new Set(documents.map(d=>d.year))].sort((a,b)=>b-a).map(y=>`<option>${y}</option>`).join('')}</select></div><div><label for="catalogue-sort">Ordre</label><select name="sort" id="catalogue-sort"><option value="relevance">Pertinence</option><option value="recent">Plus récents</option><option value="title">Titre A–Z</option></select></div><div class="library-search-actions"><label class="saved-filter"><input type="checkbox" name="saved"> Ma sélection sur cet appareil</label><button type="reset">Réinitialiser</button><button class="site-button" type="submit">Rechercher</button></div></form>
 <p id="catalogue-status" role="status" aria-live="polite" class="library-result-count">Explore le catalogue et les parcours ci-dessous.</p><div id="catalogue-results" class="catalogue-results" hidden></div><div class="catalogue-pagination" id="catalogue-pagination" hidden><button type="button" data-page="previous">← Précédent</button><span id="catalogue-page"></span><button type="button" data-page="next">Suivant →</button></div>
 <div data-catalogue-fallback><section><div class="library-section-title"><h2>Dans les archives ouvertes</h2><p>Les fichiers restent hébergés par le dépôt d’origine. Les notices sont collectées via l’API HAL ; leur présence ne constitue pas une validation scientifique.</p></div><div class="catalogue-results">${initial.map(documentCard).join('')}</div><p><a class="site-button" href="/bibliotheque/documents">Parcourir les ${number(documents.length)} documents</a></p></section>
 <section><div class="library-section-title"><h2>Guides et parcours de travail</h2></div><div class="library-grid">${pages.map(p=>`<article class="library-card" data-resource data-format="guide"><span class="resource-type">${e(p.category)}</span><h3><a href="/${p.slug}">${e(p.heading)}</a></h3><p>${e(p.lead)}</p><a class="resource-link" href="/${p.slug}">Lire le guide →</a></article>`).join('')}</div></section>
</div>
 <noscript><p>Les filtres nécessitent JavaScript. Tous les documents restent accessibles dans le <a href="/bibliotheque/documents">catalogue paginé</a>, et les guides ci-dessus se lisent librement.</p></noscript>
 </div> <section id="modeles"><div class="library-section-title"><h2>Modèles Word à adapter</h2></div><div class="library-grid">${templateResources.map(t=>`<article class="library-card template-card" data-resource data-format="template"><span class="resource-type">Modèle Word gratuit</span><h3>${e(t.title)}</h3><p>${e(t.description)}</p><a class="resource-link" href="${t.path}" download>Télécharger le modèle ↓</a><a class="template-guide" href="${t.guide}">Lire la méthode associée</a></article>`).join('')}</div></section>${renderOwnedLibrary()}<section class="library-section-title"><h2>Explorer les disciplines</h2><div class="discipline-links">${disciplines.map(d=>`<a href="${disciplinePath(d)}">${e(d.label)} <span>${documents.filter(x=>x.discipline===d.id).length}</span></a>`).join('')}</div></section><p class="fine">Collecte des notices : ${snapshot.checkedAt}. Titres, auteurs et liens issus de HAL. <a href="/methode-editoriale">Provenance et politique éditoriale</a>.</p></section></main><script src="/assets/catalogue.js" type="module"></script>`;
 return layout({title,description,meta:metadata({...context,path:'/bibliotheque',title,description,collection:initial.map(d=>({name:d.title,path:documentPath(d)}))}),content});
}
export function renderDocument(d,context){
 const s=subject(d),title=`${d.title} — ${d.year} · ${d.id}`,description=`${documentTypes[d.type]} de ${authorShort(d)}, ${d.year}. Notice bibliographique, export RIS et BibTeX, et lien vers le document d’origine sur HAL.`;
 const noteRecord=JSON.stringify({id:d.id,title:d.title,authors:d.authors,year:d.year,type:d.type,doi:d.doi,sourceUrl:d.url,fileUrl:d.file}).replaceAll('<','\\u003c');
 const license=d.license?`<a href="${e(d.license)}" target="_blank" rel="noopener noreferrer">Consulter la licence indiquée par le dépôt ↗</a>`:'Licence non précisée dans les métadonnées collectées.';
 const related=documents.filter(x=>x.discipline===d.discipline&&x.id!==d.id).slice(0,3);
 const content=`<main id="contenu"><section class="record-hero"><nav class="breadcrumbs" aria-label="Fil d’Ariane"><a href="/bibliotheque">Bibliothèque</a><span>/</span><a href="${disciplinePath(s)}">${e(s.label)}</a></nav><p class="eyebrow">${e(documentTypes[d.type])} · ${d.year} · Français</p><h1>${e(d.title)}</h1><p class="record-authors">${e(d.authors.join(', '))}</p><div class="hero-actions"><a class="site-button" href="#mes-notes">Travailler cette source ici</a><a href="${e(d.file)}" target="_blank" rel="noopener noreferrer">Lire le texte intégral chez HAL ↗</a><a href="${e(d.url)}" target="_blank" rel="noopener noreferrer">Consulter le dépôt d’origine ↗</a></div><p class="fine">Document externe. Accès selon les conditions du dépôt ; aucun fichier de l’auteur n’est vendu ni hébergé ici.</p></section><div class="record-layout"><article class="record-main"><h2>Repères bibliographiques</h2><dl class="record-metadata"><div><dt>Type</dt><dd>${e(documentTypes[d.type])}</dd></div><div><dt>Discipline de classement</dt><dd><a href="${disciplinePath(s)}">${e(s.label)}</a></dd></div><div><dt>Année</dt><dd>${d.year}</dd></div><div><dt>Identifiant HAL</dt><dd>${e(d.id)}</dd></div>${d.journal?`<div><dt>Publication</dt><dd>${e(d.journal)}</dd></div>`:''}${d.doi?`<div><dt>DOI</dt><dd>${a('https://doi.org/'+d.doi,d.doi)}</dd></div>`:''}<div><dt>Droits du document</dt><dd>${license}</dd></div></dl>${d.keywords.length?`<h2>Mots-clés du dépôt</h2><ul class="keyword-list">${d.keywords.map(k=>`<li>${e(k)}</li>`).join('')}</ul>`:''}<section class="record-reading"><p class="eyebrow">Méthode de lecture · ${e(s.label)}</p><h2>${e(s.readingTitle)}</h2><p>${e(s.reading)}</p><ol><li>Ouvre le document et retrouve sa question de recherche.</li><li>Note le matériau, la méthode et les passages qui te seront utiles.</li><li>Conserve les limites avant d’intégrer une idée à ton travail.</li></ol><p><a href="/${s.guide}">Approfondir cette méthode →</a></p><p class="fine">Ces pistes concernent la discipline ; elles ne sont pas un résumé ni une évaluation de ce document.</p></section><section id="mes-notes" class="record-note-workspace"><h2>Lire, noter et préparer ta citation</h2><p>Garde la question à laquelle tu veux répondre, la méthode du document, les pages utiles et ses limites. Sépare les citations exactes de ta reformulation et de tes commentaires.</p><div data-library-notes="${e(d.id)}"><p>Le formulaire de notes nécessite JavaScript. Tu peux aussi <a href="/modeles/fiche-lecture-source.docx" download>utiliser la fiche de lecture Word</a>.</p></div><script type="application/json" data-library-note-record>${noteRecord}</script><p><a href="/guides/fiche-lecture">Comment remplir une fiche de lecture</a> · <a href="/guides/revue-litterature">Relier cette lecture à ton mémoire</a></p></section><section><h2>Provenance de cette notice</h2><p>Les informations sont issues de l’API publique HAL, collectées le <time datetime="${d.checkedAt}">${d.checkedAt}</time>. HAL déclarait un fichier en texte intégral pour cette notice. Le titre, les auteurs, l’identifiant et les liens ont fait l’objet de contrôles automatiques de métadonnées. Le contenu scientifique de chaque PDF n’a pas été relu par Soutenance Pro.</p><p>Vérifie sur le dépôt la version, les droits et les éventuelles mises à jour avant de citer. <a href="/methode-editoriale">Comment la bibliothèque est constituée</a>.</p></section></article><aside class="record-aside"><div class="reading-card"><h2>Garder cette référence</h2><p class="record-citation">${e(referenceText(d))}</p><p class="fine">Référence de travail à adapter au style demandé. Certains champs peuvent manquer.</p><a class="site-button" href="/references/${d.id}.ris" download>Exporter vers Zotero · RIS</a><a href="/references/${d.id}.bib" download>Télécharger en BibTeX ↓</a><button class="save-reference" type="button" data-save="${d.id}" aria-pressed="false" hidden>Garder dans ma sélection</button><p class="fine">La sélection reste sur cet appareil. Les fichiers RIS et BibTeX contiennent des métadonnées.</p><a href="/modeles/fiche-lecture-source.docx" download>Fiche de lecture Word ↓</a></div></aside></div><section class="related-section"><h2>Dans la même discipline</h2><div class="catalogue-results">${related.map(documentCard).join('')}</div></section></main><script src="/assets/catalogue.js" type="module"></script>`;
 // Catalog records are useful navigation, not a mass of duplicated search landing pages.
 const meta=metadata({...context,production:false,path:documentPath(d),title,description,breadcrumbs:[{name:'Bibliothèque',path:'/bibliotheque'},{name:s.label,path:disciplinePath(s)},{name:d.id,path:documentPath(d)}]});
 return layout({title,description,meta,content});
}
export function renderDiscipline(s,context){
 const selected=documents.filter(d=>d.discipline===s.id),title=`${s.label} : documents de recherche et méthode de lecture`,description=`Explore des thèses, mémoires et articles en ${s.label.toLocaleLowerCase('fr')}. Sources HAL, références exportables et repères pour construire ta lecture.`;
 return layout({title,description,meta:metadata({...context,path:disciplinePath(s),title,description,collection:selected.map(d=>({name:d.title,path:documentPath(d)}))}),content:`<main id="contenu"><section class="article-hero"><div class="article-hero-inner"><p class="eyebrow">Bibliothèque · ${selected.length} documents référencés</p><h1>${e(s.label)}</h1><p class="article-lead">${e(s.reading)}</p><a class="site-button light" href="/${s.guide}">${e(s.readingTitle)} →</a></div></section><section class="library-content"><p class="directory-introduction">Cette sélection francophone utilise les classifications du dépôt HAL. Elle privilégie des notices récentes avec un fichier déclaré et conserve leurs auteurs et droits. Elle ne représente pas toute la littérature du domaine. Pour une recherche exhaustive, complète-la avec les <a href="/recherche">moteurs académiques</a> et les ressources de ton établissement.</p><div class="catalogue-results">${selected.map(documentCard).join('')}</div><section class="record-reading"><h2>Construire ta propre sélection</h2><p>Définis d’abord une question, une période et les types de documents utiles. Compare ensuite les méthodes, les terrains et les références citées. Garde une note sur la pertinence de chaque document au lieu d’accumuler seulement des titres.</p><a href="/guides/fiche-lecture">Préparer une fiche de lecture →</a></section><a href="/bibliotheque">Rechercher dans toute la bibliothèque →</a></section></main>`});
}
export function renderCataloguePage(n,context){
 const selected=documents.slice((n-1)*50,n*50),title=`Documents de recherche francophones${n>1?' — page '+n:''}`,description=`Catalogue de ${number(documents.length)} documents HAL avec références et accès au dépôt d’origine. Page ${n} sur ${cataloguePageCount}.`;
 return layout({title,description,meta:metadata({...context,production:context.production&&n===1,path:pagePath(n),title,description,collection:selected.map(d=>({name:d.title,path:documentPath(d)}))}),content:`<main id="contenu"><section class="record-hero"><p class="eyebrow">Bibliothèque académique</p><h1>Documents de recherche${n>1?' · Page '+n:''}</h1><p>${number(documents.length)} notices avec fichier déclaré par HAL. Les documents se consultent chez leur hébergeur ; tu peux exporter leurs références depuis chaque notice.</p><a class="site-button" href="/bibliotheque">Rechercher et filtrer les documents</a></section><section class="library-content"><nav class="catalogue-pagination" aria-label="Pages du catalogue">${n>1?`<a href="${pagePath(n-1)}">← Page précédente</a>`:'<span></span>'}<span>Page ${n} / ${cataloguePageCount}</span>${n<cataloguePageCount?`<a href="${pagePath(n+1)}">Page suivante →</a>`:'<span></span>'}</nav><div class="catalogue-results">${selected.map(documentCard).join('')}</div><nav class="page-numbers" aria-label="Accès aux pages">${Array.from({length:cataloguePageCount},(_,i)=>`<a href="${pagePath(i+1)}"${i+1===n?' aria-current="page"':''}>${i+1}</a>`).join('')}</nav></section></main>`});
}
export function referenceFiles(d){return {ris:referenceRIS(d),bib:referenceBib(d)};}
